# DiskMind 开发待办 (TODO)

> 文档版本:v1.2 · 撰写日期:2026-05-25 · 上次更新:2026-05-25 · 状态:Active
>
> 关联文档:
> - [技术设计文档 (TDS)](./2026-05-25-02-DiskMind技术设计文档.md)
> - [原型设计文档](./2026-05-25-03-DiskMind原型设计.md)
> - [可行性分析报告](./2026-05-25-DiskMind可行性分析报告.md)

---

## 0. 当前状态

**MVP 第一期已交付 (Round 4)**:Scanner / Classifier 真实落地,前后端 IPC 完整打通,扫描设置 (`localStorage` 持久化) → IPC 参数 → Rust 多 root + symlink 选项;DiskMap treemap 接真实目录占用聚合;**结果列表新增 Tree 视图** (递归汇总 fileCount / totalBytes / risks);路径 hover Tooltip + Copy 按钮;扫描中 indeterminate 流动进度条;mock 数据由 store 真实数据接管(scan store + dirSummary)。下一阶段进入 1.5 数据持久化 (rusqlite) 与 1.4 AI Provider。

> 目录变更:`prototype/` 已更名为 `app/`(2026-05-25),原 `prototype/` 残留目录已彻底删除(2026-05-25)。旧文档中的 `prototype/` 路径作为历史快照保留,不再回填。

### 设计沿革 (2026-05-25 多次迭代,按时间倒序)

**Round 4 — MVP 一期交付 (Scanner / Classifier / IPC 联调 + 体验打磨)** (本次)
- **Rust 后端**:`scanner` (walkdir + 多 root + follow_symlinks + 进度 emit + 取消信号 + 部分结果保留) + `classifier` (12 条本地规则 + >1GB 大文件兜底) + `lib.rs` 注册 `start_scan` / `cancel_scan`,完成后 emit `scan:complete` 携带 `results / dirSummary / cancelled / durationMs`
- **前端 IPC 层**:`@tauri-apps/api` + `app/src/api/tauri.ts`(StartScanArgs / DirSummary / ScanResultRow 等类型完整);`stores/scan.ts` 从 IPC event 订阅,`stores/scanSettings.ts` (Pinia + localStorage) 提供扫描目标/选项
- **结果列表 → Tree 视图**(本次新增重要功能):`lib/buildTree.ts` 把 ScanResultRow[] 转嵌套 TreeNode,聚合 fileCount/totalBytes/risks 到每一级祖先,自动折叠单子目录链;`ScanResultsTree.vue` + `TreeNode.vue` 递归组件,支持展开/折叠/选中文件/Tooltip-Copy/AI 单文件追问;在 `pages/scan/index.vue` 加 ToggleGroup 切换 树 / 表
- **DiskMap 真实数据**:Rust `build_dir_summary` 按 root 一级子目录聚合占用,前端 `DiskMapView` 改用 store.dirSummary,空态卡片
- **路径 UX**:表格 + 树 视图均使用 reka-ui Tooltip 显示完整路径 + 复制按钮(1.4s 反馈),`dir="rtl"` 截断保留尾部文件名
- **进度条**:扫描中改用自定义 indeterminate(absolute -left-1/3 w-1/3 的小条 translateX(400%) 1.4s 循环),完成后实心 100%
- **横向滚动条 / 闪烁修复**:根因是 `<SidebarInset>` 在 shadcn 默认无 `min-w-0`,加 `overflow-x-hidden` + `<main>` 同步;最终在 `assets/index.css` 给 `html, body` 加 `overflow-x: hidden` 全局兜底;`<Table>` 改 `table-fixed` 杜绝长路径撑表;`ScanProgressCard` 用 `grid-cols-[auto_minmax(0,1fr)]` 替代 flex+inline span 修截断
- **取消语义**:取消不再 `Err`,Scanner 返回 `ScanOutcome { entries, cancelled }`,emit `scan:complete` 携带 `cancelled=true`,前端展示已发现部分结果
- **登录/初次提交 + 项目托管**:Git init + push 到 https://github.com/shetengteng/DiskMind.git,根目录 `.gitignore` + `README.md` 就位;`design/logo` / `app/screenshots` 删除

**Round 3 — IA 二次合并 + 视觉系统统一**
- **菜单合并**:删除 `/diskmap` 顶级路由,磁盘地图作为扫描页内 Tab(`结果列表 / 目录地图`);主菜单从 4 项降为 3 项(仪表盘 / 扫描 / 报告)+ 底部 2 项(回收站 / 设置)
- **数据去重**:仪表盘 DataTable (Top 10 大表) → 替换为 LastScanSummary 摘要卡(高/中/低风险计数 + 跳扫描按钮);报告概览 OverviewStatsRow KPI 行删除(与仪表盘 SectionCards 重复)
- **视觉清理**:11 处装饰性紫色(`text-violet-500` / `from-violet to-fuchsia` 渐变等)→ 统一改为 `primary` / `muted` 中性色;6 处 icon-only 按钮补 aria-label;3 个孤儿组件(KpiCards / CategoryCard / DataTable)清理
- **路由防御**:`/diskmap` → 重定向到 `/scan?view=map`;`/:pathMatch(.*)*` → 兜底回 `/dashboard`,防止历史链接白屏
- **bug 修复**:lucide `Map` icon 与全局 `Map` constructor 命名冲突(`new Map() is invalid` 错误)→ 统一改 `Map as MapIcon`;Tab 切换刷页问题(`watch(resultView) → router.replace` 触发 Tauri WebView reload)→ 去掉 URL 写回,只在 mount 时单向读取 `?view`

**Round 2 — IA 一次合并 + Treemap 灰阶**
- 合并 `/scan` + `/results` 为单页,删除 `/results` 路由 + `pages/scan-results/` 目录
- 扫描目标 / 选项移到设置页"扫描"Tab(顺序:通用 > 扫描 > AI Provider > 隐私)
- Sidebar 去掉 group label,顶部 4 项 / 底部 2 项
- Treemap 配色改为 shadcn 灰阶(L-only,无色相,选中态 primary 黑/白)+ `labelInternalNodes: true` 让分组节点显示标签

**Round 1 — 整体视觉对齐 shadcn dashboard-01**
- 全局布局采用 SidebarProvider + SidebarInset + 极简 SiteHeader

### 已完成事项 (原型层)

| 模块 | 状态 | 备注 |
| --- | --- | --- |
| 全局布局 (AppLayout / AppSidebar / SiteHeader) | ✅ | shadcn-vue dashboard-01 风格,inset 侧栏,极简 header,sidebar 无 group label,头部 logo 用 SidebarMenuButton 标准布局,底部回收站/设置 |
| 仪表盘 (`/dashboard`) | ✅ | SectionCards (4 KPI) + ChartAreaInteractive (面积图,7d/30d/90d) + LastScanSummary (摘要卡 + 跳扫描按钮) |
| 扫描页 (`/scan`) | ✅ | 扫描头部 + ScanProgressCard + done 阶段 Tabs (`结果列表 / 目录地图`),idle 显示空状态卡引导去设置 |
| 磁盘地图 (扫描页 Tab) | ✅ | `@unovis/vue` `VisTreemap` (squarified),shadcn 灰阶 (L-only) 配色 + 选中黑/白,文字标签可见;不再是顶级菜单 |
| 沙箱回收站 (`/trash`) | ✅ | 表格 + 三段操作 (恢复/立即删除/清空) + 确认对话框 |
| 报告 (`/reports`) | ✅ | 3 Tabs (概览 / AI 使用 / 历史),概览只留 趋势 + 分类占比(去除冗余 KPI) |
| 设置 (`/settings`) | ✅ | 4 Tabs (通用 / 扫描 / AI Providers / 隐私),扫描目标+选项已迁入此页,主题切换可用 |
| 主题系统 (`composables/useTheme.ts`) | ✅ | auto/light/dark 三模式,localStorage 持久化,跟随系统 |
| AI Drawer | ✅ | `⌘L` 触发,Mock 流式响应,引用面板 |
| 视觉系统 (shadcn 极简) | ✅ | 全局装饰性紫色清零,只余数据可视化合理用色(类目色板 + 风险状态色 + 完成态绿);6 处 a11y aria-label |

### 已知瑕疵 (不阻塞开发,但需要在迭代中清理)

- [ ] `RecoveryTrendChart.vue` 的 tooltip selector 在 dev console 报错 (`'.[data-vis-grouped-bar]' is not a valid selector`),不影响渲染但需排查
- [ ] SiteHeader 极简化后丢失:面包屑 / 全局搜索 / 主题快捷切换 / AI 按钮入口,目前 AI 仅 `⌘L` 可触发
- [ ] `tsconfig.app.json` 的 `baseUrl` 在 TypeScript 6 被标记 deprecated,`vue-tsc` 警告但不影响构建
- [ ] 面积图 30d/90d 模式下 mock 数据是 7d 重复扩展,真实集成时需要后端真实序列
- [ ] mock 数据 (`src/data/mock.ts`) 全是写死的中文目录与路径,集成阶段全部要由真实扫描结果取代
- [ ] 扫描页 `?view=map` query 参数仅在 mount 读一次,Tab 切换不会写回 URL(刷新页面会回到默认 list,Tauri 桌面端不影响)
- [ ] `LastScanSummary` 当前直读 mock,接 Tauri 后台后应读 scan store 的最新结果(空态显示"还未扫描")

---

## 1. P0 — 必须做 (MVP 阻塞项)

### 1.1 Tauri 2.0 集成

- [x] 新增 `src-tauri/` 目录,初始化 Tauri 2.0 项目骨架 (2026-05-25)
- [x] `tauri.conf.json` 配置:`identifier=com.diskmind.app`,窗口 1280×800,min 1024×640,`productName=DiskMind`
- [x] 验证:`pnpm tauri:dev` 能启动桌面窗口并加载 `http://localhost:5173/` 现有前端 (2026-05-25)
- [x] 安装 `@tauri-apps/api`,前端封装 IPC 调用层 (`src/api/tauri.ts`) (2026-05-25 Round 4)
- [x] 主进程 IPC 通道定义:`start_scan` (StartScanArgs) / `cancel_scan`,event `scan:progress` / `scan:complete` / `scan:error` (2026-05-25 Round 4)
- [ ] `Cargo.toml` 元信息打磨 (`name=app` → `diskmind`,`description`、`authors`、`repository`)
- [ ] 前端 `vite.config.ts` 增加 Tauri 专属配置 (`server.strictPort: true`,`HMR` 在 Tauri 内嵌 webview 下的 hostname 处理)
- [ ] 应用图标与 bundle 资源 (替换 Tauri 默认 icon set)

### 1.2 Scanner 模块 (Rust)

- [x] 文件遍历:基于 `walkdir`,多 root 输入,可选 `follow_symlinks`,跳过 `.git/.svn/.idea/...` (2026-05-25 Round 4)
- [x] 元数据采集:`size / mtime / extension / is_symlink` (`atime / mode` 后续补) (2026-05-25 Round 4)
- [x] 进度回报:每 200 个文件 emit `scan:progress`,扫描完成 / 取消额外 emit 终态进度 (2026-05-25 Round 4)
- [x] 取消信号:`Arc<AtomicBool>` 共享,取消时返回 `ScanOutcome { entries, cancelled=true }` 保留部分结果 (2026-05-25 Round 4)
- [ ] 增量扫描:与上次 Bundle diff,只重算变化文件 (依赖 1.5 落库)
- [ ] 单测:典型目录结构 + 大文件 + 符号链接 + 隐藏文件
- [ ] 大目录并行 (Rayon),目前是单线程遍历

### 1.3 Classifier 模块

- [x] 内置规则库 (12 条):浏览器缓存 / `node_modules` / Xcode DerivedData / Cargo cache / `.DS_Store` / 日志 / `.Trash` / 安装包 (dmg/pkg/iso) / 大型媒体 / **>1GB 兜底"待审查大文件"** (2026-05-25 Round 4)
- [x] 路径模式匹配 (`/library/caches/...` / `/node_modules/` 等) + 扩展名匹配 (2026-05-25 Round 4)
- [x] 输出 `ScanResultRow` (字段对齐前端 schema),按 `sizeBytes` 倒序 + Top 500 (2026-05-25 Round 4)
- [x] 大文件 (>1GB) 未命中其他规则也作为高风险候选(避免遗漏个人大文件)(2026-05-25 Round 4)
- [ ] 规则库迁移到外部 `rules/*.toml` 加载与热更新
- [ ] 应用映射 (`com.apple.Safari` → 浏览器缓存),与 macOS bundle id 映射

### 1.4 AIEngine 模块

- [ ] LiteLLM 桥接 (DeepSeek-V3 / OpenAI / Anthropic 三家最低)
- [ ] 本地 Ollama 兜底链路
- [ ] Prompt 编排 (扫描分类 / 风险问询 / 清理决策 / 报告解读 4 场景)
- [ ] 结果校验 (JSON Schema validation,失败降级)
- [ ] Token 计数与 cost 累计 (写入 `aiCallLogs`)
- [ ] 隐私模式 (路径脱敏)

### 1.5 数据持久化

- [ ] SQLite Schema 创建 (`bundles / scan_results / trash_items / ai_call_logs / providers / settings`)
- [ ] DuckDB 初始化 (用于报告聚合查询)
- [ ] 迁移脚本 v1 → v2 占位
- [ ] 备份/导出能力 (`Bundle` 导出为 JSON / CSV)

### 1.6 Trash 沙箱

- [ ] 应用内回收站独立于系统 Trash
- [ ] 移动到沙箱:仅记录元数据,实际文件 hardlink 到 `~/.diskmind/trash/`
- [ ] 30 天自动清理 (后台任务)
- [ ] 恢复:hardlink 还原 + DB 状态回滚

### 1.7 前后端联调 (替换 mock.ts)

- [x] `src/api/tauri.ts` 完整封装 startScan / cancelScan + 3 类 event 订阅 (2026-05-25 Round 4)
- [x] `stores/scan.ts` 重写 - 调真 IPC + 订阅 progress/complete/error 事件,暴露 `results` / `dirSummary` / `wasCancelled` (2026-05-25 Round 4)
- [x] `stores/scanSettings.ts` (Pinia + localStorage) - 扫描目标 / 选项持久化 (2026-05-25 Round 4)
- [x] 错误处理:`scan:error` 事件 → store.errorMessage → UI subtitle 展示 (2026-05-25 Round 4)
- [x] `pages/scan/index.vue` / `LastScanSummary` / `HighRiskCard` / `DiskMapView` 全部接 store 数据,空态降级 (2026-05-25 Round 4)
- [x] 结果列表 Tree 视图(本次新增):`lib/buildTree.ts` + `ScanResultsTree.vue` + `TreeNode.vue` 递归组件,fileCount/totalBytes/risks 逐级汇总,自动折叠单子目录链 (2026-05-25 Round 4)
- [ ] 错误 toast(目前是 subtitle 文本展示,未来用 sonner / shadcn toast)
- [ ] AI Drawer / Reports / Settings 等次要页面仍引用 `data/mock.ts`(待 1.4/1.5 接入后清理)

---

## 2. P1 — 应该做 (上线前体验完善)

### 2.1 设置功能补齐

- [ ] AI Provider 增删改查 (UI 已就位,接 Rust keychain 存 API Key)
- [ ] LiteLLM 路由规则 / Fallback Chain 真实生效
- [ ] 隐私设置 Tab 的开关接入真实行为 (脱敏 / 不上传 / 仅本地)
- [ ] 通用 Tab 的开关 (开机自启 / 托盘 / 启动时扫描) 接入 Tauri API

### 2.2 SiteHeader 增强

- [ ] 重新加入 AI 触发按钮 (右上角,带状态点)
- [ ] 全局搜索 (Cmd+K Command Palette,Reka UI Combobox)
- [ ] 面包屑根据路由 meta.breadcrumb 动态生成 (代码已留接口)

### 2.3 i18n

- [ ] 安装 `vue-i18n`,创建 `locales/zh-CN.json` / `en.json`
- [ ] 所有硬编码中文字符串迁移到字典 key
- [ ] 设置 → 语言下拉去掉 disabled,接入 i18n 切换
- [ ] 后续语言:zh-TW / ja (需翻译)

### 2.4 自动更新

- [ ] Tauri Updater 集成
- [ ] 签名:macOS notarization / Windows code signing
- [ ] 更新通道 (stable / beta)

### 2.5 错误监控

- [ ] Rust panic 捕获 → 写本地日志 + 用户同意后上报 (Sentry / 自建)
- [ ] 前端 `onErrorCaptured` 收口
- [ ] 崩溃报告 dialog (设置 → 通用 → 发送匿名崩溃报告 已就位)

---

## 3. P2 — 可以做 (优化 / 探索)

### 3.1 性能

- [ ] 大目录扫描 worker 化 (Rayon 并行)
- [ ] DuckDB 报告查询优化 (索引 / 分区)
- [ ] 前端 treemap / 表格虚拟滚动 (`@tanstack/vue-table` 已装但未启用 virtualization)

### 3.2 视觉打磨

- [x] 清理 dashboard 孤儿组件 (KpiCards / CategoryCard / DataTable 已删) (2026-05-25 Round 3)
- [x] 全局装饰性紫色清零,统一为 shadcn 黑白纯极简 (2026-05-25 Round 3)
- [x] icon-only 按钮 a11y aria-label 补齐 (2026-05-25 Round 3)
- [ ] DiskMap treemap 支持嵌套层级 (`Movies > old-trip-2019.mov`),需扩展 `mock.ts` 与后端 schema
- [ ] 报告页 RecoveryTrendCard 改为面积图,与 dashboard 视觉统一
- [ ] 暗色模式下 chart palette 微调 (部分 oklch 在 dark 下饱和度偏低)
- [ ] DiskMap treemap 当前是单层,真实数据接入后需要支持点击下钻(子目录树)

### 3.3 高级特性

- [ ] 重复文件检测 (BLAKE3 + 局部哈希两阶段)
- [ ] 文件指纹增量更新
- [ ] AI Agent 多轮对话 (当前 Drawer 只支持单次问答)
- [ ] 跨设备同步 (云盘配置共享 / cleaning history)
- [ ] 插件机制 (规则库由社区扩展)

### 3.4 测试

- [ ] Playwright e2e 覆盖 4 条核心 flow:扫描 → 结果 → 沙箱 → 恢复
- [ ] Rust 模块单测覆盖率 ≥ 70%
- [ ] LLM Prompt 回归测试 (snapshot of expected JSON output)

---

## 4. 里程碑建议

| 里程碑 | 目标 | 状态 | 预估周期 |
| --- | --- | --- | --- |
| **M1** Tauri 骨架 + IPC | 1.1 + 1.7 部分 | ✅ 完成 (2026-05-25) | 1 周 |
| **M2** Scanner + Classifier + 真实数据替换 mock | 1.2 + 1.3 + 1.7 完成 | ✅ MVP 一期完成 (2026-05-25 Round 4) | 2 周 |
| **M2.5** Tree 视图 + DiskMap 真实数据 + UX 打磨 | 当前阶段交付物 | ✅ 完成 (2026-05-25 Round 4) | — |
| **M3** 数据持久化 + AI Engine + 沙箱 | 1.5 + 1.4 + 1.6 | ⏳ 进行中 (1.5 next) | 2 周 |
| **M4** 设置功能 + 自动更新 + 内测包 | 2.1 + 2.4 | 🟡 待启动 | 2 周 |
| **M5** i18n + Header 增强 + 错误监控 | 2.2 + 2.3 + 2.5 | 🟡 待启动 | 1.5 周 |
| **M6** 性能优化 + 重复文件 + 公开 Beta | P2 优先项 | 🟡 待启动 | 2 周 |

总计约 **10.5 周** 到达公开 Beta,与可行性报告中的 12 周排期吻合。

### 当前阶段进度 (Round 4 验收点)

- [x] 真实扫描:`pnpm tauri:dev` 启动 → 设置勾选 ~ → 点扫描 → 实时进度 → 完成出 Top 500 候选
- [x] 取消保留:扫描中点终止,phaseLabel 显示"已取消 (部分结果)",已发现候选保留可见
- [x] 树视图:扫描完成后默认 Tree 视图,目录展开/折叠,每个目录显示 N 文件 + 总占用 + 占主导风险
- [x] DiskMap 真数据:目录地图 Tab 显示家目录子目录占用,treemap 矩形面积 ∝ 实际占用
- [x] 路径 Hover Tooltip + Copy 按钮(列表 + 树双视图均可用)
- [x] 横向滚动条根除 (html/body overflow-x-hidden + table-fixed + grid minmax)

---

## 5. 责任人 (待填)

| 模块 | 负责人 | 备注 |
| --- | --- | --- |
| Tauri / Rust 后端 | TBD | |
| 前端 (Vue/UI) | TBD | |
| AI Engine | TBD | |
| 数据 / DuckDB | TBD | |
| 测试 / QA | TBD | |
| 设计 / 视觉 | TBD | |
