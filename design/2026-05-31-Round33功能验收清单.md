# Round 33 + 34 功能验收清单

> 文档版本:v1.2 · 撰写日期:2026-05-31 · 关联交付:Round 32 + Round 33 + Round 34A + Round 34C (Sprint A + B + C + Housekeeping + R34A ScanOptions 接通 + R34C 撤回编辑器)
> 关联 commit:`<R34C>` + `937f496` (验收清单 v1.1) + `d65757d` (R34) + `7faad27` (R33 docs) + `0ea35ac` (R33 feat) + `3c3909e` (R32 docs / landing) · 远端:`origin/main` 已同步
> 关联待办:[`2026-05-25-05-DiskMind开发待办-95%.md`](./2026-05-25-05-DiskMind开发待办-95%.md) · §0 当前状态

本清单按"先快后慢、先自动后手动"的顺序排布。从第 1 节走到 §R34A 大约 30 分钟,**自动化测试**(§2)5 分钟就能拿到一个明确的 PASS/FAIL,UI 手验是核心验收(§3 / §5 / §R34A)。

**v1.2 更新**:Round 34C 应用户反馈撤回 R34B 可视化规则编辑器 + R32 的 Privacy Tab 用户规则卡片(reveal/reload),保留 R34A(ScanOptions 接通 BLAKE3 自动)。验收 §R34B 与 §3.x 用户规则 UI 步骤已下线,本轮 v1.2 同步调整。

**v1.1 更新(历史)**:Round 34 上线两件事 — (a) ScanOptions「自动检测重复文件」开关从装饰改成可用;(b) 用户规则改 UI 编辑器。Round 34C 后 (b) 部分已撤回,本节仅作为变更历史保留。

---

## 0. 本轮交付概览

| Sprint | 交付物 | 主要文件 | 验收节 |
| ------ | ------ | -------- | ------ |
| A (Round 32) | 用户自定义规则 UI 入口(reveal + reload + i18n) | `pages/settings/components/PrivacySettingsTab.vue`, `api/tauri.ts`, i18n × 2 | §3 |
| B (Round 33) | DiskMap 嵌套下钻补测 + 待办校准 | `pages/disk-map/components/DiskMapView.spec.ts` (新) | §4 |
| C (Round 33) | **S14 BLAKE3 双阶段重复文件检测主线**(M6 核心) | `scanner/dedup.rs`(新)、`commands/dedup.rs`(新)、`stores/dedup.ts`(新)、`pages/scan/components/DuplicateGroupsView.vue`(新)、IPC / i18n / 状态机 / 11 + 9 个测试 | §5 |
| Housekeeping | 6 个 pre-existing typecheck 错误清零 | `CommandPalette.vue`、`ScanResultsTable.vue`、`AiCleaningAdviceCard.spec.ts`、`ai.spec.ts` | §6 |
| 取消项 | `跨设备同步` / `插件机制` 永久撤销(理由见待办 §3.3) | — | §8 |
| **R34A** | ScanOptions `computeHash` 砍掉 + `detectDuplicates` 接通 → 扫描后自动 BLAKE3 | `ScanOptionsCard.vue`、`scan.ts`、`scanSettings.ts`、i18n × 2 | §R34A |
| ~~R34B~~ | ~~用户规则可视化编辑器~~ — **R34C 已撤回**(用户反馈"挺复杂") | — | (已下线) |
| **R34C** | 撤回 R32 + R34B 全部用户规则 UI 入口,保留 R29 后端模块(只在 setup 启动时读 TOML 一次)| 删 `UserRulesEditorDialog.vue` + `commands/classifier.rs`,改回 `PrivacySettingsTab.vue` / `api/tauri.ts` / `user_rules.rs` / `lib.rs` / `commands/mod.rs` / i18n × 2 | §R34C |

---

## 1. 环境准备

> 已经能跑 `pnpm tauri:dev` 的环境可以跳过本节。

```bash
node -v       # 期望 v22.x 或更高
pnpm -v       # 期望 9.x 或更高
rustc -V      # 期望 1.80+
cargo -V      # 与 rustc 同版本

cd /Users/TerrellShe/Documents/personal/tt-projects/DiskMind/app
pnpm install  # 首次或拉完 commit 后建议跑一次
```

- [ ] **A0**:`pnpm install` 退出码为 0,无 `ERR_` 输出
- [ ] **A1**:`cd app/src-tauri && cargo check` 成功,无 `error[`,允许有 warning(`cargo check` 比 `cargo build` 快 ~3 倍,用于验依赖图)

---

## 2. 自动化测试快速门(~5 分钟)

> 这一节是后续手动验收的"地基":如果这里挂了,后面的 UI 验收一定也挂。**绿则继续,红则查日志并报回**。

### 2.1 前端单测

```bash
cd app
pnpm test --run
```

- [ ] **B1**:Test Files **10 passed**,Tests **108 passed**(99 旧 + 9 dedup store)
- [ ] **B2**:DiskMapView.spec.ts 输出 5 个 `✓`
- [ ] **B3**:dedup.spec.ts 输出 9 个 `✓`
- [ ] **B4**:无 `RED` / `FAIL`,无 `unhandled error`

> Round 34 暂未新增前端测试(Round 35 计划补 `UserRulesEditorDialog.spec.ts` + scan store auto-dedup spec),所以总数仍是 108。

### 2.2 前端类型 + 构建

```bash
pnpm exec vue-tsc --noEmit
pnpm build
```

- [ ] **B5**:`vue-tsc --noEmit` 退出码 0,无 `error TS`
- [ ] **B6**:`pnpm build` 退出码 0,**6 个 pre-existing 错误全部消失**(参考 §6 详单)
- [ ] **B7**:`dist/` 生成,索引 HTML + JS chunk 存在

### 2.3 后端单测 + clippy

```bash
cd app/src-tauri
cargo test --lib
cargo clippy --lib -- -D warnings
```

- [ ] **B8**:`cargo test --lib` 输出 `test result: ok. 92 passed`(81 旧 + 11 dedup;R34C 已撤回 3 个 R34B save 测试)
- [ ] **B9**:`scanner::dedup::tests::*` 出现 11 个 `... ok` 行
- [ ] **B10**:`classifier::user_rules::tests::save_then_load_roundtrip_preserves_all_matcher_kinds` / `save_empty_set_then_load_returns_empty` / `save_to_creates_parent_dir_when_missing` 各出现 `... ok`
- [ ] **B11**:`cargo clippy --lib` 退出码 0(dedup + user_rules 模块 0 警告)

> 若 §2 所有 check 通过,后续节点失败概率 < 5%。

---

## 3. Sprint A 验收:用户自定义规则 UI(~3 分钟)

### 3.1 启动 app

```bash
cd app
pnpm tauri:dev
```

等待"Listening on http://localhost:1420" 与 Tauri 窗口同时出现。

### 3.2 进入 Settings → Privacy

操作:左侧 sidebar 点 **Settings**(齿轮图标)→ 顶部 tab 切到 **Privacy / 隐私**。

- [ ] **C1**:页面看到"**用户自定义规则**"卡片(图标:`FileCog`),位置在"沙箱目录"卡片下方
- [ ] **C2**:卡片描述说"用户规则优先级高于内置 builtin 规则,只追加不覆盖"
- [ ] **C3**:"规则文件位置"行下方有一个灰底等宽路径(macOS 形如 `/Users/.../Library/Application Support/com.diskmind.app/rules.toml`)
- [ ] **C4**:卡片底部有两个按钮:"**在 Finder 中打开**"(`FolderOpen` 图标)、"**重新加载**"(`RefreshCw` 图标)
- [ ] **C5**:卡片底部状态文本默认显示"rules.toml 不存在或为空,只走 builtin 规则"(因为大概率没创建过)

### 3.3 验证「在 Finder 中打开」

操作:点 **在 Finder 中打开**。

- [ ] **C6**:Finder / Explorer 弹出,定位到 `rules.toml` 所在父目录(`com.diskmind.app/`)
- [ ] **C7**:无 toast 报错(若 rules.toml 不存在,自动 fallback 到父目录,这是预期行为)

### 3.4 验证「重新加载」+ toast 反馈

操作 1:不创建 rules.toml,直接点 **重新加载**。

- [ ] **C8**:按钮内 spinner 短暂 spin,toast 弹出"已加载 0 条用户规则"(rules.toml 不存在视为 0 条)
- [ ] **C9**:状态文本更新成"已加载 0 条用户规则"(从空态切到显式 0)

操作 2:在第 3.3 步打开的目录中手动创建 `rules.toml`,内容如下:

```toml
[[rule]]
pattern = "*.tmp"
category = "Cache"
priority = 50

[[rule]]
pattern = "*.bak"
category = "Cache"
priority = 50
```

回到 app,再次点 **重新加载**。

- [ ] **C10**:toast 弹出"已加载 2 条用户规则",状态文本同步显示"已加载 2 条用户规则"

### 3.5 验证 i18n 切换

操作:Settings → General → Language 切到 English / 中文,回到 Privacy tab。

- [ ] **C11**:"用户自定义规则" 在 EN 模式下显示为 "User-defined classifier rules" 等英文文案
- [ ] **C12**:按钮文案 / 状态文本 / 描述全部切语言无中英混杂

---

## 4. Sprint B 验收:DiskMap 嵌套下钻补测(~2 分钟)

> 注:Sprint B 本轮没有新功能上线,而是**校准了待办**(发现下钻在 Round 4 / 23 / 29 已实现)+ **补了测试锁住行为**。验收主要看测试 + 走一遍交互确认未回归。

### 4.1 测试输出验证

```bash
cd app
pnpm test --run app/src/pages/disk-map/components/DiskMapView.spec.ts
```

- [ ] **D1**:输出 5 个 `✓`,分别覆盖
  - empty results 渲染 `EmptyState`
  - 初始 root 渲染 Treemap
  - `drillInto` 推 drillStack + breadcrumb 增长 + `ArrowUp` 按钮可见
  - `drillUp` / `jumpTo` 重置 stack
  - `drillInto` 未知节点是 no-op

### 4.2 UI 回归走查

操作:左侧 sidebar 点 **Disk Map** → 等待"扫描"或选一个有数据的最近扫描。

- [ ] **D2**:出现 Treemap 视图,顶部有"扫描根路径"breadcrumb(单层)
- [ ] **D3**:点 Treemap 上某个目录 tile → breadcrumb 末尾增加该目录名;view 切到该目录下钻视图
- [ ] **D4**:出现 **ArrowUp**(向上箭头)按钮
- [ ] **D5**:点 breadcrumb 中间的某一段 → view 跳回该层级,breadcrumb 后续段消失
- [ ] **D6**:无扫描结果时,中心区域显示 EmptyState 文案("尚未扫描"等)

---

## 5. Sprint C 验收:BLAKE3 重复文件检测(核心,~15 分钟)

> M6 主线核心交付。需先准备一个能产生重复文件的目录。

### 5.1 准备测试样本(选其一)

**方案 A**:用 `/Downloads` 或 `/Pictures` 等真实场景(推荐)。

**方案 B**:手动构造一组重复文件(更可控):

```bash
mkdir -p /tmp/diskmind-dedup-test
cd /tmp/diskmind-dedup-test
# 8 MB 随机文件,复制 3 份(同内容 / 不同名)
dd if=/dev/urandom of=base.bin bs=1M count=8 2>/dev/null
cp base.bin copy1.bin
cp base.bin copy2.bin
# 另一组,5 MB 副本 ×2
dd if=/dev/urandom of=other.bin bs=1M count=5 2>/dev/null
cp other.bin other-copy.bin
# 噪声文件(独一无二,不会进任何组)
dd if=/dev/urandom of=unique.bin bs=1M count=3 2>/dev/null
ls -lh
```

预期:5 个 8 MB + 5 MB + 5 MB + 8 MB + 8 MB + 3 MB = 6 个文件,**期望产生 2 个重复组**(8 MB × 3 / 5 MB × 2),浪费 = 8×2 + 5×1 = **21 MB**。

### 5.2 扫描 + 切到「重复文件」Tab

操作:

1. 左侧 sidebar 点 **Scan / 扫描**
2. 路径输入框 `/tmp/diskmind-dedup-test`(或方案 A 选的路径),点"开始扫描"
3. 等扫描 phase 切到 **done**,文件列表出现
4. 顶部 tabs 中点 **Duplicates / 重复文件**(`Copy` 图标)

- [ ] **E1**:Tab 切换无报错,URL 变成 `...?view=duplicates`
- [ ] **E2**:页面中央显示"开始查找重复文件"按钮 + 候选数量提示("scan.results 中有 N 个候选")

### 5.3 启动检测 + 验证三阶段进度

操作:点 **开始查找重复文件**。

- [ ] **E3**:按钮变成 **取消**(`Square` 图标),进度条从 0 开始
- [ ] **E4**:进度条文案按以下顺序出现(短目录可能太快看不全,可参考下文 console):
  1. "按文件大小分组中 (N 个候选)"
  2. "计算文件头部哈希 (X / Y)"
  3. "计算完整文件哈希 (X / Y)"(若所有文件 ≤ 64KB 则跳过本阶段)
- [ ] **E5**:进度条数值随 processed 增长,**单调不回退**

> 想要看清三阶段切换,打开 DevTools (Cmd+Option+I) → Console,会看到 `dedup:progress { stage: 'size' | 'head' | 'full', ... }` 事件流。

### 5.4 验证完成态 + 分组展示

- [ ] **E6**:检测完成后,顶部出现"已完成"摘要:"找到 2 组重复(共 5 个文件),可节省 ~21 MB,用时 X.XX 秒"
- [ ] **E7**:下方出现 2 个 collapsible 分组,**按 wastedBytes 降序**:
  - 第一组:8 MB × 3 副本(`16 MB 可节省`)
  - 第二组:5 MB × 2 副本(`5 MB 可节省`)
- [ ] **E8**:每组标题显示 `8 MB × 3 副本 · ~16 MB 可节省 · #<hash 前 16 位>`
- [ ] **E9**:展开某组,看到 3 行文件:文件名 + 父目录 + 全路径(hover 看 tooltip)+ 「在文件管理器中显示」(`FolderOpen`)+ 行首 Checkbox

### 5.5 验证文件级 reveal

操作:点任意一行的 **FolderOpen** 图标。

- [ ] **E10**:Finder 弹出,文件已被高亮选中
- [ ] **E11**:回到 app 无 toast 错误

### 5.6 验证选中态 + 批量移入沙箱

操作:

1. 在第一组,勾选 2 行(留一份原始)
2. 在第二组,勾选 1 行
3. 注意顶部统计:"已选中 3 个文件,可释放 ~21 MB"
4. 点 **批量移入沙箱(3 项)**

- [ ] **E12**:勾选行后,"批量移入沙箱"按钮显示选中数量
- [ ] **E13**:组级 checkbox 在"部分勾选"时显示 indeterminate 状态(中间横线)
- [ ] **E14**:点 **批量移入沙箱** 后,toast 提示成功,3 个文件从硬盘消失
- [ ] **E15**:左侧 sidebar **Trash Sandbox**(沙箱)tab 出现这 3 个文件,可恢复
- [ ] **E16**:Scan 页 results 自动 reload(`trash:changed` 事件),被回收的行变灰 / 消失
- [ ] **E17**:Duplicates 页面分组仍在(快照不变),只是被回收的行不能再勾选

### 5.7 验证「重新运行」+ 重置态

操作:点页面上方 **重新运行**(`RotateCcw` 图标)。

- [ ] **E18**:groups 清空 → 进度条重新跑(因 scan.results 已少了 3 个文件,候选量减少)
- [ ] **E19**:重新完成后,组数 / 浪费空间数同步更新(预期:1 组 8MB × 1 + 5MB × 1 都不再重复 → 0 组,或仍剩 1 组,取决于上一步具体怎么删)
- [ ] **E20**:UI 显示"未找到重复文件"空态文案(若 0 组)

### 5.8 边界:取消运行

操作:点 **重新运行** 后立刻点 **取消**(`Square`)。

- [ ] **E21**:进度条停止,phase 显示"已取消",按钮变回 **重新运行**
- [ ] **E22**:取消后 1 秒内 UI 响应(latency ≤ 一次 hash 时间,几百 ms)
- [ ] **E23**:已扫描的部分数据不展示(取消语义:丢弃中间结果)

### 5.9 边界:无候选 / 空 results

操作:Scan 一个空目录(`mkdir /tmp/empty && cd /tmp/empty`),切到 Duplicates tab。

- [ ] **E24**:页面显示"没有可用候选(请先完成一次扫描)"
- [ ] **E25**:"开始查找重复文件"按钮被禁用 + tooltip 解释

### 5.10 边界:i18n 切换

操作:在 Duplicates 页面 i18n 切到 English。

- [ ] **E26**:进度文案 / 按钮 / 摘要 / 错误态 / 空态 全部切英文(36 条新 key 都生效)
- [ ] **E27**:`hash_prefix` 显示为 `#aaaaaaaaaaaaaaaa`(16 位 hex,UI 稳定 key)

---

## 6. Housekeeping 验收:6 个 pre-existing 错误清零

> 验法:对照 `git diff 3c3909e..0ea35ac` 看下面 4 个文件,然后 §2.2 `pnpm build` 已经验过编译通过。

```bash
git log --oneline 3c3909e..0ea35ac     # 应只出现 0ea35ac 一条
git diff 3c3909e..0ea35ac -- \
  app/src/components/layout/CommandPalette.vue \
  app/src/pages/scan/components/ScanResultsTable.vue \
  app/src/pages/reports/components/AiCleaningAdviceCard.spec.ts \
  app/src/stores/ai.spec.ts | head -120
```

- [ ] **F1**:`CommandPalette.vue` 192/208 行可见 null guard + i18n marker `$i18n:common.desktopRequired` throw
- [ ] **F2**:`ScanResultsTable.vue` 178 行 `:key="String(vrow.key)"`(从原 `:key="vrow.key"` 改的)
- [ ] **F3**:`AiCleaningAdviceCard.spec.ts` mock 改成 `vi.fn<MockedApi['xxx']>()` 显式泛型
- [ ] **F4**:`ai.spec.ts` 同上,`aiCleaningAdvice` / `aiCleaningAdviceGet` 显式泛型
- [ ] **F5**:`pnpm build` 输出无 `error TS2345 / TS2322 / TS2493`(就是这 6 个错误的编号)

---

## R34A 验收:ScanOptions「扫描后自动检测重复文件」开关接通(~5 分钟)

> 这一节验证 §0 表里 "R34A" 那行的实际效果:用户期望"勾上开关 → 扫描完不必切 Tab 主动启动,Duplicates 数据自然出现"。

### R34A.1 进入 Settings → 扫描

操作:左侧 sidebar 点 **Settings → 扫描**。

- [ ] **R1**:看到 `扫描选项` 卡片,**只有 3 个开关**(`跟随符号链接` / `扫描后自动检测重复文件` / `扫描后自动 AI 打标签`)
- [ ] **R2**:**「计算 SHA-256 哈希」整个开关已删除**(R34A 砍掉,这是关键回归点)
- [ ] **R3**:`扫描后自动检测重复文件` 开关旁边的 Copy 图标可见,**无「规划中」徽章** / **无灰显**(已可用)
- [ ] **R4**:描述文案为 "扫描完成后,自动用 BLAKE3 双阶段哈希(大小分组 → 头部 64KB → 全文)..."(若 EN 切换:"After each scan, automatically runs the two-stage BLAKE3...")
- [ ] **R5**:开关可点击切换,刷新页面后状态保留(localStorage persist)

### R34A.2 验证自动触发

操作:

1. **开启**「扫描后自动检测重复文件」开关
2. 回 Scan 页,确保扫描根设到准备好的 `/tmp/diskmind-dedup-test`(或 §5.1 方案)
3. 点 **开始扫描**

- [ ] **R6**:扫描走完 phase 切到 `done` 后,**自动出现「重复文件」Tab 的进度条**(无需用户切到 Duplicates Tab 主动启动)
- [ ] **R7**:打开 DevTools Console,可见 `[scan] auto dedup failed`(若失败)或者 `[dedup] backend accepted scan_detect_duplicates`(成功),证明 auto-trigger 走通
- [ ] **R8**:切到 Duplicates Tab,看到检测完成或进度中,无需手动点 Start
- [ ] **R9**:**关闭**开关后再扫描一次,Duplicates Tab **不会**自动启动,与 R6 形成对照

### R34A.3 旧数据迁移验证

> 此项验证 `normalizeOptions` 把老 localStorage 里的 `computeHash` 字段静默丢弃。

操作:

1. 退出 app,在 DevTools(或 Application → Local Storage)中找到 `diskmind:scanSettings`,把值改成包含 `computeHash` 字段的 JSON:

```json
{"targets":[],"options":{"computeHash":true,"detectDuplicates":false,"aiAnalysis":false,"followSymlinks":false,"excludeSensitive":false},"bootstrapped":true}
```

2. 重启 app

- [ ] **R10**:app 不报错,Settings → 扫描页正常打开
- [ ] **R11**:DevTools Application → Local Storage 中 `diskmind:scanSettings.options` **没有 computeHash 字段**(已被静默 strip)

---

## R34C 验收:撤回用户规则可视化编辑器 + R32 reveal/reload UI(~5 分钟)

> 这一节验证 R34B(编辑器)+ R32(reveal/reload)从 UI 完全下线后,功能没有残留入口且后端仍正常工作。

### R34C.1 Privacy Tab 不再出现"用户自定义规则"卡片

操作:左侧 sidebar 点 **Settings → 隐私**,从上滚到底。

- [ ] **C1**:页面**没有任何**"用户自定义规则" / "User-defined rules"卡片
- [ ] **C2**:页面**没有任何**"打开规则编辑器" / "Open rule editor"按钮
- [ ] **C3**:页面**没有任何**"在 Finder 中打开" / "Reveal"或"重新加载" / "Reload"按钮(原 R32 入口已撤)
- [ ] **C4**:其它隐私设置(沙盒路径、崩溃日志路径)**仍在**且工作

### R34C.2 后端模块仍正常加载内置 + 可选 user rules

操作:重启 app(`pnpm tauri dev` 重启)。

- [ ] **C5**:首页 Dashboard / 扫描 / 报表都能正常加载,无 panic
- [ ] **C6**:对一个含 macOS 浏览器缓存的目录跑全盘扫描,结果里 `category` 字段仍能识别出 `browser_cache`(说明 builtin 19 条 bundle id + 12 条路径规则正常匹配)
- [ ] **C7**:**高级用户路径**:手工创建 `<app_data>/rules.toml`(macOS:`~/Library/Application Support/diskmind/rules.toml`),写一条:
  ```toml
  [[rule]]
  id = "custom_test"
  category = "browser_cache"
  risk = "low"
  reason_key = "classifier.reason.browser_cache_chrome"

  [rule.matcher]
  kind = "path_contains"
  value = "/Library/Caches/MyCustomApp/"
  ```
  然后**重启 app**(R34C 砍了 reload IPC,需要重启),再扫描含 `MyCustomApp/Caches` 的目录,该路径仍能匹配到 `browser_cache` 分类。

### R34C.3 IPC 端口已注销

操作:启动 app,在 Vue DevTools / 浏览器控制台执行:

```js
import('@tauri-apps/api/core').then(c => c.invoke('classifier_user_rules_path')).then(console.log).catch(e => console.error('expected fail:', e))
```

- [ ] **C8**:控制台报错 `Command classifier_user_rules_path not found`(说明 4 个 R29/R34B IPC 全部撤注册)
- [ ] **C9**:同样测试 `classifier_reload_user_rules` / `classifier_list_user_rules` / `classifier_save_user_rules`,均报 not found

### R34C.4 自动化测试

```bash
cd app/src-tauri
cargo test --lib classifier 2>&1 | tail -10
```

- [ ] **C10**:`test result: ok. N passed`,且**不再出现** `save_then_load_roundtrip_preserves_all_matcher_kinds` / `save_empty_set_then_load_returns_empty` / `save_to_creates_parent_dir_when_missing` 这三个 R34B 测试名

```bash
cd app
pnpm test --run 2>&1 | tail -3
```

- [ ] **C11**:`Test Files <N> passed | Tests 108 passed`(前端测试数不变)

### R34C.5 i18n key 已清除

```bash
cd app
rg -n 'settings\.privacy\.userRules|settings\.userRulesEditor' src/ 2>&1 | tail -5
```

- [ ] **C12**:**无任何匹配**(零代码引用 + 零 i18n 字典残留)

```bash
rg -n 'classifierListUserRules|classifierSaveUserRules|classifierUserRulesPath|classifierReloadUserRules|UserRuleSet|UserRuleMatcher' src/ 2>&1 | tail -5
```

- [ ] **C13**:**无任何匹配**(API wrapper + 类型完全删干净)

---

## 7. Git 状态验证

```bash
cd /Users/TerrellShe/Documents/personal/tt-projects/DiskMind
git log --oneline -5
git status -sb
git rev-parse origin/main
git rev-parse HEAD
```

- [ ] **G1**:`git log --oneline -8` 顶部六条(从新到旧)应包含:
  - `<R34C>` revert: roll back R34B visual rule editor + R32 reveal/reload UI
  - `937f496 docs: update acceptance checklist for Round 34`(v1.1 历史)
  - `d65757d feat(ux): wire R33 dedup to ScanOptions + visual rule editor`(R34A + R34B,部分已被 R34C 撤回)
  - `7faad27 docs: add Round 33 acceptance checklist...`(本清单 v1.0)
  - `0ea35ac feat(s14): BLAKE3 two-stage duplicate detection + classifier rules UI`(R33)
  - `3c3909e docs: rewrite README with full pitch + add English variant + landing page`(R32 docs)
- [ ] **G2**:`git status -sb` 显示 `## main...origin/main`(干净,无 ahead/behind)
- [ ] **G3**:`git rev-parse origin/main == git rev-parse HEAD`(本地与远端同步)
- [ ] **G4**:`git show --stat <R34C>` 应显示删除 `UserRulesEditorDialog.vue` / `commands/classifier.rs`,修改 `user_rules.rs` / `lib.rs` / `commands/mod.rs` / `PrivacySettingsTab.vue` / `api/tauri.ts` / `i18n/*` + design docs
- [ ] **G5**:`git show --stat 0ea35ac` 显示 22 files changed / +2215 / -27
- [ ] **G6**:`git show --stat 3c3909e` 应包含 `README.md` / `README.en.md` / `docs/` 树

---

## 8. 已知遗留 / 永久取消

### 8.1 推迟到 Round 35(原 Round 34 计划被 R34A/R34B UX 整改占用)

- **scan_result 加 `hash_head` / `hash_full` 缓存列 + dup_group 关系**:首版 BLAKE3 6 GB/s 够快(2000 候选 ~ 秒级),真要做缓存等用户实际抱怨"反复扫慢"。
- **dedup 端到端测试**(硬链接 / 跨卷 / 截断 / power-loss):单测已覆盖核心算法(11 个),端到端需要管理员权限 + 多卷挂载 + 模拟掉电,CI 不稳定,等集成测试环境一起做。
- **关键 UI 组件单测补测**(CommandPalette / BreadcrumbBar / TrashTable / ScanProgressCard):本轮未上,但不影响发布,这些组件历史回归率低,后续 Round 35 一并做。
- **R34A 新增推迟项**:
  - **scan store auto-dedup 行为 spec** — 验证 `detectDuplicates=true && results.length>0` 时 `dedup.detect` 被调用,反向 no-op。需先搭 dynamic-import mock。
  - ~~`UserRulesEditorDialog.spec.ts`~~ — R34C 已撤回编辑器,此项作废。

### 8.2 永久取消(经过推理,见待办 §3.3 详细)

- **跨设备同步(云盘配置 / cleaning history)**:违背"数据不出本机"的隐私承诺。用户需要时自行复制 `diskmind.sqlite`。
- **插件机制(WASM / .so 加载)**:Round 29 的 `rules.toml` 加载仍然覆盖了"高级用户扩展规则库"这个场景(R34C 撤了 UI 后仍可手编 TOML,启动时加载),引入 plugin runtime 会带来沙箱逃逸风险,ROI 倒挂。
- **R34B 可视化规则编辑器**:R34C 已撤回(用户反馈"挺复杂"),不再重新实现。后续若需要,简化方案是"只读列表展示内置 19+12 条规则",而非允许 CRUD。

---

## 9. 单条 PASS / FAIL 反馈模板

如果哪一条 check 未通过,请按以下格式回报:

```text
- 失败编号:E14(批量移入沙箱)
- 操作复述:勾选 3 个文件 → 点「批量移入沙箱(3 项)」
- 期望:toast 成功 + 文件从硬盘消失
- 实际:toast 弹出但提示 "trash store unavailable"
- 截图 / 日志:<贴 console error / Tauri stderr>
```

我会据此定位 commit 内细节,1-2 个回合内补上。

---

## 10. 一键综合验收命令(可选)

> 跑完后看输出尾部,如果 `cargo test --lib` 与 `pnpm test --run` 与 `pnpm build` 都退出码 0,§2 自动门全过。

```bash
cd /Users/TerrellShe/Documents/personal/tt-projects/DiskMind/app && \
  pnpm test --run && \
  pnpm exec vue-tsc --noEmit && \
  pnpm build && \
  cd src-tauri && \
  cargo test --lib && \
  cargo clippy --lib -- -D warnings && \
  echo "=== Round 33 + 34A + 34C 自动化验收 PASS ==="
```

走完上面 + §3 / §5 / §R34A / §R34C 的 UI 手验,即完成本轮 100% 验收。
